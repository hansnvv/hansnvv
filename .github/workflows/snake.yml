name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=6e1a1a&color_dots=#d0d0d0,#b0b0b0,#888888,#555555,#222222
            dist/github-contribution-grid-snake-dark.svg?color_snake=6e1a1a&color_dots=#2a2a2a,#4a4a4a,#7a7a7a,#aaaaaa,#f0ebe3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
